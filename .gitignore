_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock
vendor/
.DS_Store

# Working documents, deliberately untracked. This repo is public if it serves
# GitHub Pages on a free account, and Jekyll's `exclude` only stops a file
# being served — not being read in the repo.
product-soc2-ai-pack.md
paper-revision-plan.md
backups/
