source "https://rubygems.org"

# Matches what GitHub Pages runs, so a local preview is the real thing.
#   bundle install && bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick"
