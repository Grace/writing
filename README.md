# posts

Source for the site. Markdown in, Jekyll out, served by GitHub Pages.

Local preview:

    bundle install
    bundle exec jekyll serve

`_config.yml` excludes working documents that are not for publication.
Check that list before adding a file here.

Before the first deploy, set `url` in `_config.yml` to whatever this actually
serves from. Every canonical tag is built from it.
